import React from "react";
import ChooseSlot from "./ChooseSlot";

function Faculty(props)
{
    return (
        <>
        <h3>faculty wala pageh h ye bauwa</h3>
        {/* {props.arr.map(x =>console.log(x))} */}
        {console.log(props.arr)}
        </>
        
        )
}

export default Faculty;